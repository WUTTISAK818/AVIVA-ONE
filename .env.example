# ===== WinVote — ตัวแปรสภาพแวดล้อม (แยกอิสระจาก aviva-private) =====
# คัดลอกไฟล์นี้เป็น .env.local แล้วเติมค่าจริง

# --- Supabase (ฐานข้อมูล WinVote โดยเฉพาะ — คนละ project กับ aviva-private) ---
# ค่าเหล่านี้เป็น public client key (ปลอดภัยตามดีไซน์ ป้องกันด้วย RLS)
NEXT_PUBLIC_SUPABASE_URL=https://gfnelofmgzqfwvlbaabd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdmbmVsb2ZtZ3pxZnd2bGJhYWJkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODA1NzEwMDAsImV4cCI6MjA5NjE0NzAwMH0.zpAG-5MorIEhBjd21V5XTl6snJ_RWDewV9jqR0NfyOQ

# --- ความลับฝั่ง server (อย่า commit ค่าจริง — ตั้งใน Netlify/Vercel env เท่านั้น) ---
# Service Role key: Supabase Dashboard ของ project WinVote -> Settings -> API -> service_role
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (อ่านบัตร ปชช ด้วย Vision) — ไม่มีก็ได้ (จะ fallback ให้กรอกเอง)
OPENAI_API_KEY=

# LINE OA (เฟส 2 ยืนยันเบอร์) — ตั้งตอนพร้อมเปิดใช้
LINE_OA_ID=
LINE_CHANNEL_SECRET=

# --- โหมดเดโม (สำคัญ) ---
# ตั้ง =1 เพื่อทดสอบ UI ด้วยข้อมูลจำลอง (ข้าม Supabase auth + ไม่อ่าน DB จริง)
# *** Production ต้องปล่อยว่างหรือ =0 เท่านั้น *** ไม่งั้นข้อมูลจริง 185 หน่วยจะไม่แสดง
NEXT_PUBLIC_DEMO_MODE=
